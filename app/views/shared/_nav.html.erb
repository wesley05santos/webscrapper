<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to  "Navbar", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Profile
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to  "Create", new_profile_path, class: "dropdown-item" %></li>
            <li><a class="dropdown-item" href="#">ReScan</a></li>
            <li><%= link_to  "All Profiles", profiles_path, class: "dropdown-item" %></li>
          </ul>
        </li>
      </ul>
      <li class="d-flex" role="search">
        <%= form_with url: search_profiles_path, method: :get do |form| %>
          <%= form.text_field :query, placeholder: 'Name, URL,.....' %>
          <%= form.submit "🔍" %>
        <% end %>
      </li>
    </div>
  </div>
</nav>
